import { Button } from "./ui/button";

const MainNav = () => {
  return (
    <Button
      variant="ghost"
      className="font-bold hover:text-black/60 hover:bg-[#ffff00]/80 bg-[#FFFF00]"
    >
      Log In
    </Button>
  );
};

export default MainNav;
